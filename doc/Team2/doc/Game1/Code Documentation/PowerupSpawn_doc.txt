PowerupSpawn - Lew

Variables: Uses a public array of gameobjects to store the various powerups

OnTriggerEnter(): If the obstacle collides with the player, bullet, or laser, the obstacle will break and
spawn a powerup in its place. It will spawn a random object from the array once destroyed.